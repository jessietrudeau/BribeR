% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_transcripts.R
\name{read_transcripts}
\alias{read_transcripts}
\title{Read transcript files into an Arrow Table}
\usage{
read_transcripts(
  dir,
  pattern = "\\\\.(csv|tsv)$",
  recursive = FALSE,
  keep_original_speaker = FALSE,
  skip_on_missing = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{dir}{Character string. Path to the directory containing transcript files.}

\item{pattern}{Regex pattern for matching files. Defaults to \code{"\\\\.(csv|tsv)$"}.}

\item{recursive}{Logical. Should subdirectories be searched? Default is \code{FALSE}.}

\item{keep_original_speaker}{Logical. If \code{TRUE}, include the original \code{speaker}
column (if present) in the output. Default is \code{FALSE}.}

\item{skip_on_missing}{Logical. If \code{TRUE}, files missing required columns
are skipped with a warning. If \code{FALSE}, the function stops with an error.
Default is \code{TRUE}.}

\item{quiet}{Logical. If \code{FALSE}, print a message for each loaded file.
Default is \code{TRUE}.}
}
\value{
An \code{arrow::Table} with the following columns:
\itemize{
\item \code{file_id} : transcript identifier (filename without extension)
\item \code{speaker_std} : standardized speaker name
\item \code{text} : transcript text
\item \code{speaker} : included only if \code{keep_original_speaker = TRUE} and present in source
}
}
\description{
Loads all \code{.csv} and \code{.tsv} transcript files from a directory into a single
\code{arrow::Table}. Each transcript is expected to contain at least the columns:
\itemize{
\item \code{speaker_std} : standardized speaker identifier
\item \code{speech} : transcript text, which will be renamed internally to \code{text}
}

A new column \code{file_id} is added, taken from the basename of the file
(without the extension). Optionally, the raw \code{speaker} column can be
preserved if it exists in the input.
}
