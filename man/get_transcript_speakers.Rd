% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_transcript_speakers.R
\name{get_transcript_speakers}
\alias{get_transcript_speakers}
\title{Get transcripts each speaker appears in}
\usage{
get_transcript_speakers(n = NULL, topic = NULL)
}
\arguments{
\item{n}{Optional numeric vector of transcript IDs to restrict results to
(e.g., \code{1}, \code{c(1, 5, 10)}).}

\item{topic}{Optional character vector of one or more topic names (e.g.,
\code{"media"}, \code{c("reelection", "state_capture")}). The \code{topic_} prefix is
added automatically if not included. Only transcripts where all of these
topics are flagged are included.}
}
\value{
A tibble with columns:
\itemize{
\item \code{speaker_std} (character): standardized speaker identifier
\item \code{transcripts} (list): sorted numeric vector of transcript IDs where the speaker appears
}
}
\description{
Loads the bundled \code{speakers_per_transcript} dataset and returns one row per
unique speaker with a list-column of transcript IDs (\code{n}) where that speaker
appears. Optionally filters to only transcripts matching specific IDs and/or
topics.
}
\details{
All filters are combined with AND logic: a transcript contributes speakers
only if it matches the specified IDs \strong{and} has \strong{every} specified topic
flagged. When only one filter is provided, it is applied alone. When neither
is provided, all speakers across all transcripts are returned. Requesting a
combination that never co-occurs returns a zero-row tibble.
}
\examples{
# Get all speakers and their transcript appearances
speakers <- get_transcript_speakers()
head(speakers)

# Get speakers from specific transcripts
get_transcript_speakers(n = c(1, 5))

# Get speakers from transcripts about media
get_transcript_speakers(topic = "media")

# Get speakers from transcripts about both media and reelection
get_transcript_speakers(topic = c("media", "reelection"))

# Get speakers from transcript 4, which is also about media
get_transcript_speakers(n = 4, topic = "media")

}
\seealso{
\code{\link[=read_transcripts]{read_transcripts()}}, \code{\link[=get_transcript_id]{get_transcript_id()}}, \code{\link[=get_transcripts_raw]{get_transcripts_raw()}}
}
